<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= hotel.name %> - Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="/stylesheets/hotels.css" rel="stylesheet" />
  </head>
  <body>
    <%- include("partials/navbar") %>

    <div class="container">
      <h2 class="p-3 text-center"><%= hotel.name %></h2>
      <p class="text-center">Location: <strong><%= hotel.location %></strong></p>

      <h3 class="mt-4">Available Rooms</h3>
      <div class="list-group">
        <% if (hotel.Rooms && hotel.Rooms.length > 0) { %> <% hotel.Rooms.forEach(room => { %>
        <div class="row px-3 py-2 w-100 align-items-center">
          <span class="col py-2 bg-light border rounded"> Capacity: <%= room.capacity %> | Price: $<%= room.price %> </span>
          <a href="/reservations/book?room_id=<%= room.id %>" class="col-auto btn btn-success mx-1">Book Now</a>
        </div>
        <% }) %> <% } else { %>
        <p class="text-danger">No rooms available in this hotel.</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
