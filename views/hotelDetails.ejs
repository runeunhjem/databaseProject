<!DOCTYPE html>
<html>
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <h2 class="text-center text-primary"><i><%= hotel.name %></i></h2>
    <h3 class="text-center">Location:</h3>
    <h4 class="text-center text-primary"><i><%= hotel.location %></i></h4>
    <h3 class="text-center">Rating:</h3>

    <% if (hotel.avgRating === "No ratings yet") { %>
        <h4 class="text-center text-muted"><i>No ratings yet</i></h4>
    <% } else { %>
        <h4 class="text-center text-primary"><i><%= hotel.avgRating %> ⭐</i></h4>
    <% } %>

    <% if (!hotel.rated) { %>
        <div class="text-center">
            <button onclick="makeRate(1, '/hotels/<%= hotel.id %>/rate')" class="btn btn-success">Rate this hotel</button>
        </div>
    <% } %>

    <script src="/javascripts/makeRate.js"></script>
</body>
</html>
