<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body class="d-flex flex-column min-vh-100">
    <%- include("partials/navbar") %>

    <div class="container text-center mt-5">
      <h1 class="page-title">Welcome to Hotel Booking</h1>

      <% if (user) { %>
        <div class="user-details">
          <h2>Hello, <%= user.firstName %> <%= user.lastName %>!</h2>
          <p>Email: <strong><%= user.email %></strong></p>
          <p>Role: <strong><%= user.role %></strong></p>
          <a href="/users/<%= user.id %>" class="btn btn-primary">My Profile</a>
        </div>
      <% } else if (bestHotel) { %>
        <div class="best-hotel">
          <h2>Top-rated Hotel</h2>
          <h3><%= bestHotel.name %></h3>
          <p>Location: <%= bestHotel.location %></p>
          <p>Average Rating: <%= bestHotel.avgRating %> ⭐</p>
          <a href="/hotels/<%= bestHotel.id %>" class="btn btn-primary">View Hotel</a>
        </div>
      <% } else { %>
        <h3 class="text-warning">No hotels available at the moment.</h3>
      <% } %>
    </div>

    <%- include("partials/footer") %>
  </body>
</html>
